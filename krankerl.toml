# SPDX-License-Identifier: AGPL-3.0-or-later
# Reproducible packaging config for `krankerl package`.
# https://github.com/ChristophWurst/krankerl

[package]
before_cmds = [
    # Remove stale hashed chunks from earlier builds so only current assets ship.
    "rm -rf js css",
    "composer install --no-dev --optimize-autoloader",
    "npm ci",
    "npm run build",
]

exclude = [
    "/.git",
    "/.github",
    "/.tx",
    "/build",
    "/node_modules",
    "/src",
    "/tests",
    "/vendor/bin",
    "/.editorconfig",
    "/.eslintrc.cjs",
    "/.gitattributes",
    "/.gitignore",
    "/.npmrc",
    "/.php-cs-fixer.dist.php",
    "/composer.json",
    "/composer.lock",
    "/krankerl.toml",
    "/Makefile",
    "/package.json",
    "/package-lock.json",
    "/playwright.config.js",
    "/psalm.xml",
    "/stylelint.config.cjs",
    "/tsconfig.json",
    "/vite.config.js",
    "/vitest.config.js",
    "/DataForms-User-Manual.docx",
    "*.map",
]
